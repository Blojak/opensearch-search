# Core
opensearch-py==2.8.0
pydantic-settings==2.6.1
Flask==3.1.0
# OpenAPI/Swagger UI for the Flask API (bundles the UI locally, no CDN)
flasgger==0.9.7.1

# PostgreSQL (metadata store - source of truth)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.18.5

# Embeddings (local, multilingual)
sentence-transformers==3.3.1

# Offline language detection for ingested documents
langdetect==1.0.9

# Optional simple PDF text (out of scope for the core PoC, but lightweight)
pypdf==5.1.0
